package org.scaladebugger.macros.freeze

import org.scalatest.{FunSpec, Matchers}

class FreezableSpec extends FunSpec with Matchers {
  describe("@Freezable") {
    it("should generate a companion object with a freeze method") {
      fail()
    }

    it("should modify an existing companion object with a freeze method") {
      fail()
    }

    it("should convert @CannotFreeze methods to throw an exception") {
      fail()
    }

    it("should convert @CanFreeze(Normal) methods to store ") {
      fail()
    }
  }
}
