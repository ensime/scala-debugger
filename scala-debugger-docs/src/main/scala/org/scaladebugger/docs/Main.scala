package org.scaladebugger.docs

/**
 * Main entrypoint for generating docs.
 */
object Main {
  def main(args: Array[String]): Unit = {
    // Parse CLI arguments
    val config = new Config(args)
  }
}
